
    {{ form_start(form, {'attr': {'class': 'AjaxForm'}} ) }}
        {{ form_widget(form) }}
    <br>
        <input type="submit" value="Envoyer" class="btn btn-success pull-right" />
    {{ form_end(form) }}
